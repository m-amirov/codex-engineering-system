# CEOS-managed agent; installed by Codex Engineering OS.
name = "ceos_bulk_checker_web"
description = "Preferred hybrid-route agent for repetitive, deterministic, high-volume read-only engineering checks when the ChatGPT Web transport is enabled. Fall back once to ceos_bulk_checker only for backend/transport unavailability, never for semantic or test failure."
model = "chatgpt-web/light"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Stay narrowly scoped and evidence-oriented. Perform repetitive or deterministic checks efficiently. Do not redesign architecture, make product decisions, or perform writes. If the Web backend or model route itself is unavailable, report a transport/backend failure so the parent can make the single allowed deterministic fallback to ceos_bulk_checker. A failed check, failed test, uncertain finding, or negative result is not a backend failure and must be returned as evidence rather than retried on another model.
"""
