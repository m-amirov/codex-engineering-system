{
  "name": "codex-engineering-os",
  "version": "0.3.3",
  "description": "A global-first engineering operating layer for Codex with hybrid native/ChatGPT-Web routing, skills, policies, profiles, gates and evidence.",
  "type": "module",
  "private": true,
  "bin": {
    "ceos": "./bin/ceos.mjs"
  },
  "scripts": {
    "test": "node --test",
    "self-test": "node ./bin/ceos.mjs self-test",
    "doctor": "node ./bin/ceos.mjs doctor",
    "lint": "node --check ./bin/ceos.mjs && node --check ./src/ceos.mjs && node --check ./src/yaml-lite.mjs && node --check ./src/web-preflight.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
