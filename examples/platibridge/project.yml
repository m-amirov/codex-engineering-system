version: 1
profile: platibridge
commands:
  lint: npm run lint
  test: npm test
  build: npm run build
gates:
  verification: [lint, test, build]
  release: [lint, test, build]
production:
  access: read-only
